# MongoDB
MONGO_URI="mongodb+srv://<username>:<password>@<cluster-name>.mongodb.net/?retryWrites=true&w=majority"
DB_NAME="video_analytics_db"

# Threat detection runtime
# Camera index (0,1,...) or absolute path to a video file
THREAT_VIDEO_SOURCE=0
# Path to YOLO pose model; falls back to this if env is not set
THREAT_MODEL_PATH="yolov8s-pose.pt"
# Image size used by YOLO (int)
THREAT_IMG_SIZE=768
# Detection confidence threshold (float 0..1)
THREAT_CONF=0.15

# Test harness (optional)
# Used by security_test_threat_detection.py if you run it directly
VIDEO_SOURCE=""
